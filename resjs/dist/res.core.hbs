function(root, init) {
    var q = init('{{authHeader}}', '{{urlPrefix}}');
    var r = null;
    {{#each resources}}
    r = root.{{@key}} = {};
        {{#each this}}
        r.{{@key}} = q('{{url}}', '{{method}}');
        {{/each}}
    {{/each}}
}
